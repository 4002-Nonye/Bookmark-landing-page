import React from "react";

const Section = ({ src, children, secClassName = "" }) => {
  return <section className={secClassName}>

    
    
    
    
    {children}</section>;
};

export default Section;
